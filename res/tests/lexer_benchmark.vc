
fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}


fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}

fun foo(x: T, y: U, z: V): Array(R) = {
    let a = 1, 2, 3, 4
    let b = read(x)
    if (cond(x, y)) {
        do_stuff()
        other_stuff()
    } else {
        // a comment
        whatever()
    }
    a + b ^ 2
}
